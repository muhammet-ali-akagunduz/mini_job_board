# MySQL Veritabanı Bağlantı Ayarları
spring.datasource.url=jdbc:mysql://localhost:3306/minijobboard?serverTimezone=UTC&useSSL=false
spring.datasource.username=root
spring.datasource.password=root

# Hibernate ve JPA Ayarları
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Security Configuration for Development (Varsayılan olarak Security i rahatlatmak için)
# Daha sonra Week 9'da security ekleyeceğiz
# spring.security.user.name=admin
# spring.security.user.password=admin
